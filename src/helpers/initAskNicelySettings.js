export const initAskNicelySettings = () => {
	window.asknicelySettings = {
    	autoload: false,
    	domain_key: "nassauvisiongroup",  // Domain Key - required
    	force: 1  // Only set this true for testing to the popup so it will ALWAYS appear
    };
}